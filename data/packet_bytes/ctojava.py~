import sys
import os.path

for filename in os.listdir('~/Desktop/big\ data/'):
	f = open(filename, 'rw')
	ctr = 0

	for line in f:
public byte[] pkt0 = {
			f.write('byte[] pkt'+str(ctr)+' = {')
			ctr += 1
	
public int numPkt = 1;
